CREATE TABLE PLAY_EVOLUTIONS ( ID INT NOT NULL, HASH VARCHAR(255) NOT NULL, APPLIED_AT TIMESTAMP NOT NULL DEFAULT CURRENT_TIMESTAMP, APPLY_SCRIPT VARCHAR(32000), REVERT_SCRIPT VARCHAR(32000), STATE VARCHAR(255) DEFAULT NULL, LAST_PROBLEM VARCHAR(32000), PRIMARY KEY (ID))
