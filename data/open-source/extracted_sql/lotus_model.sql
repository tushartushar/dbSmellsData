CREATE TABLE `schema_migrations` (`filename` varchar(255) NOT NULL PRIMARY KEY)
CREATE TABLE reviews ( id integer NOT NULL, title text NOT NULL, rating integer DEFAULT 0)
CREATE TABLE `reviews` (`id` integer NOT NULL PRIMARY KEY AUTOINCREMENT, `title` varchar(255) NOT NULL, `rating` integer DEFAULT (0))
CREATE TABLE schema_migrations ( filename text NOT NULL)
SELECT * FROM users"` or `BookRepository.execute "UPDATE users SET admin = 'f'
