SELECT MIN(value) FROM achievements WHERE location = ? AND id <> (SELECT MAX(id) FROM achievements)
SELECT filename FROM achievements WHERE id = ?
SELECT MAX(epoch) FROM rating_epochs)
select room from the list"));
select record from the list"));
SELECT MAX(epoch) FROM rating_epochs))
