CREATE TABLE dataset ( id INTEGER PRIMARY KEY, name text, raw_data text, created datetime DEFAULT CURRENT_TIMESTAMP)
CREATE TABLE open_dataset ( id INTEGER PRIMARY KEY, dataset_id INTEGER UNIQUE REFERENCES dataset(id) ON DELETE CASCADE ON UPDATE CASCADE, created datetime DEFAULT CURRENT_TIMESTAMP)
CREATE TABLE open_dataset ( id INTEGER PRIMARY KEY, dataset_id INTEGER REFERENCES dataset(id) ON DELETE CASCADE ON UPDATE CASCADE, created datetime DEFAULT CURRENT_TIMESTAMP)
