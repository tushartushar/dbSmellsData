SELECT accountId FROM account WHERE ownerId = :id
SELECT * FROM customer WHERE name = ? ORDER BY CUST_ID
SELECT * FROM customer WHERE name = '" + safeName + "' ORDER BY CUST_ID
SELECT * FROM customer WHERE name = '" + name + "' ORDER BY CUST_ID
