CREATE TABLE IF NOT EXISTS offers(	id BIGINT(20) AUTO_INCREMENT PRIMARY KEY,	memberId VARCHAR(32),	subject VARCHAR(512),	gmtUpdated TIMESTAMP default CURRENT_TIMESTAMP())
SELECT memberId FROM offers where id=
select name from followers where name='fname'
select name from followers where name='" + name + "'
select subject from offers where memberId=
select count(1) from followers
select tweet from tweets where tweet='tcontent'
CREATE TABLE IF NOT EXISTS tweets(id BIGINT IDENTITY PRIMARY KEY, tweet VARCHAR(140))
select * from followers where name=?
CREATE TABLE IF NOT EXISTS cobarha(timeflag TIMESTAMP)
select id from offers where memberId=
select * from followers where id=?
select name from followers where name='Darren'
select name from followers where name='" + nameStartsWithS + "'
CREATE TABLE IF NOT EXISTS followers(id BIGINT IDENTITY PRIMARY KEY,name VARCHAR(255))
CREATE TABLE IF NOT EXISTS offers(id BIGINT(20) AUTO_INCREMENT PRIMARY KEY, memberId VARCHAR(32), subject VARCHAR(512), gmtUpdated TIMESTAMP default CURRENT_TIMESTAMP())
