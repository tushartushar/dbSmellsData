SELECT * FROM #{@db.sep_table}#{@db.escape_table(@table_name)}#{@db.sep_table} WHERE
CREATE TABLE #{@db.sep_table}#{@db.escape_table(newname)
SELECT * FROM mysql.user
SELECT id FROM test
SELECT * FROM #{table_esc} WHERE #{table_esc}.#{col_esc} IN (#{ids_sql})
SELECT * FROM test
SELECT * FROM users WHERE name = '#{db.esc(name)}' ORDER BY age
SELECT * FROM users
