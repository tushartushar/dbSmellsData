UPDATE effort_log_line_items SET course_id = 55  WHERE course_id = 66
SELECT * FROM faculty_assignments WHERE faculty_assignments.course_id = #{course_id} AND faculty_assignments.person_id = #{faculty_id}&quot;
SELECT * FROM faculty_assignments WHERE faculty_assignments.course_id = #{course_id} AND faculty_assignments.person_id = #{faculty_id}
