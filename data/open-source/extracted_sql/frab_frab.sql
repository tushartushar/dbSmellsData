UPDATE conference SET acronym = replace where conference_id=86
SELECT conference_id FROM call_for_papers AS cfp WHERE cfp.id=#{n.call_for_papers_id}) WHERE id=#{n.id};
SELECT * FROM event_image WHERE event_id = #{event['event_id']}
SELECT * FROM conference
SELECT title, url FROM event_link WHERE event_id = #{orig_id}
select notification_id,locale,accept_subject,reject_subject,accept_body,reject_body from notification_translations;
SELECT * FROM person_image WHERE person_id = #{person['person_id']}
