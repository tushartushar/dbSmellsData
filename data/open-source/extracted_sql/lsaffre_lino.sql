CREATE TABLE Images_Tags ( tag_id INTEGER, image_id INTEGER )
SELECT * FROM Images WHERE album_id = "'.$o->id.'"');
select * from " + this.name;
CREATE TABLE Images ( id INTEGER PRIMARY KEY, filename STRING, url STRING, album_id INTEGER, description TEXT )
SELECT COUNT(id) FROM demo WHERE 
CREATE TABLE `demo` ( `id` int(10) unsigned NOT NULL auto_increment, `price` float NOT NULL, `company` varchar(255) NOT NULL, `date` datetime NOT NULL, `size` varchar(45) NOT NULL, `visible` tinyint(1) NOT NULL, PRIMARY KEY (`id`))
select * from Albums');
SELECT id FROM Tags WHERE text = "'.$tag.'"');
SELECT url FROM Images WHERE id = "'.$image.'"');
select * from Images')->fetchAll());
SELECT * FROM Albums where id = "'.$data->id.'"');
SELECT * FROM demo WHERE 
SELECT * FROM Tags';
CREATE TABLE Albums_Images ( album_id INTEGER, image_id INTEGER )
SELECT * FROM Albums';
SELECT url FROM Images where id ="'.$images[$i].'"');
CREATE TABLE Tags ( id INTEGER PRIMARY KEY, text STRING )
CREATE TABLE Albums ( id INTEGER PRIMARY KEY, text STRING, created STRING, description TEXT )
CREATE TABLE IF NOT EXISTS ', tableName, ' (', buf.join(','), ')
SELECT * FROM Images WHERE filename = "'.$name.'"');
