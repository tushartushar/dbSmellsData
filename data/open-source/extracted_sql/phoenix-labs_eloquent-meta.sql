CREATE TABLE meta( id INTEGER PRIMARY KEY NOT NULL, metable_id INTEGER NOT NULL, metable_type TEXT NOT NULL, key TEXT NOT NULL, value TEXT NOT NULL)
select * from custom_model_meta');
select * from tests where name = ?', ['Nicole']);
