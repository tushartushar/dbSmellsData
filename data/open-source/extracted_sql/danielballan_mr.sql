SELECT * FROM </span><span class="si">%s</span><span class="s">&quot;
SELECT %s FROM %s WHERE FRAME = %d
CREATE TABLE blacklist (	revision VARCHAR(50) NOT NULL, 	PRIMARY KEY (revision))
SELECT * FROM %s
