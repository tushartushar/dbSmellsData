update contacts set first_name = :first_name, last_name = :last_name, email = :email  where short_name = :short_name
update contacts set first_name = :first_name, last_name = :last_name, email = :email  where short_name = :short_name
select * from contacts&quot;</span><span class="br0">&#41;</span>;
select * from contacts
CREATE TABLE contacts ( id SERIAL, short_name VARCHAR(100) NOT NULL, first_name VARCHAR(100) NOT NULL, last_name VARCHAR(100) NOT NULL, email VARCHAR(100) NOT NULL, UNIQUE (short_name))
select * from contacts where short_name = :short_name
select * from contacts where short_name = :short_name&quot;</span><span class="br0">&#41;</span>;
