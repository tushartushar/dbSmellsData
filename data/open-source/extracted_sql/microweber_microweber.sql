select * from "content" limit 1
Select page from your site",
SELECT * FROM ' . $table);
SELECT option_group FROM $table WHERE module IS NULL
SELECT * FROM sqlite_master WHERE type='table';
SELECT * from $table where " . $par_q . $content_type_q . " and is_deleted=0 $is_shop order by position desc limit 0,100
CREATE TABLE IF NOT EXISTS '{get_table_prefix()}_countries' ( id int(11) NOT NULL, code TEXT NOT NULL, name TEXT NOT NULL, continent LONGTEXT NOT NULL, surfacearea float NOT NULL, population int(11) NOT NULL, localname TEXT NOT NULL )
SELECT * from $table where id={$parent} and is_deleted=0 
SELECT * FROM ' . $table;
create index jobs_queue_reserved_reserved_at_index on "localhost_jobs
SELECT * FROM '.$table;
select id, parent_id from $table where id = $id and data_type='{$data_type}' " . $with_main_parrent_q;
Select page from your site
SELECT * FROM sqlite_master WHERE type='table' and (tbl_name='{$full_table_name}');
SELECT * FROM $table WHERE id IN (
SELECT * FROM $table WHERE id=$parent 
CREATE TABLE IF NOT EXISTS {MW_TABLE_PREFIX}countries ( id int(11) NOT NULL, code TEXT NOT NULL, name TEXT NOT NULL, continent LONGTEXT NOT NULL, surfacearea float NOT NULL, population int(11) NOT NULL, localname TEXT NOT NULL ,PRIMARY KEY (id) )
SELECT * FROM $table WHERE parent_id=$parent AND data_type='category' AND is_deleted=0 
CREATE TABLE IF NOT EXISTS {get_table_prefix()
SELECT id, parent FROM $table WHERE parent={$id} " . $with_main_parrent_q;
SELECT id, parent_id FROM $table WHERE parent_id=$parent_id ";
SELECT * FROM ' . $table . " where rel_type='content' ";
CREATE TABLE {TABLE_PREFIX}module_stats_users_online ( id int(11) NOT NULL auto_increment, created_by integer, view_count integer, user_ip varchar(15) NOT NULL default '', visit_date date default NULL, visit_time time NOT NULL default '00:00:00', last_page varchar(35) NOT NULL default '', PRIMARY KEY (id), UNIQUE KEY id (id) )
SELECT id FROM $table WHERE id=1 ORDER BY updated_at DESC LIMIT 0,1 
select * from "content" where "content"."id" = ? limit 1
SELECT * FROM $table WHERE id=$parent AND is_deleted=0 
SELECT * FROM $table WHERE parent_id=$parent AND is_deleted=0 AND data_type='category' AND (category_subtype='$content_type' OR category_subtype='inherit' ) 
