CREATE TABLE commands (math_environment NUMERIC, command TEXT, command_group TEXT, description TEXT)
SELECT command, command_group, math_environment FROM commands
SELECT command, command_group FROM commands
