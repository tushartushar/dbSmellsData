select expand(other) from ClassA where name = :name
select * from OUser where @rid = 
select expand(empty) from ClassA where name = :name
select expand(empty) from ClassA
select expand(other) from ClassA
